{
  "packages": ["packages/*"],
  "npmClient": "yarn",
  "version": "independent",

  "command": {
    "bootstrap": {
      "hoist": true
    },
    "version": {
      "message": "chore(release): publish",
      "ignoreChanges": ["**/*.md", "**/*.mdx"]
    }
  }
}
